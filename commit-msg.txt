feat: discord admin self-governing panel, agent reassign fix, dedup events

- Discord bot admin channel now shows self-governing status panel with
  start (modal with goal + timer), stop, and refresh buttons
- Admin panel edits in place (single persistent message, no spam)
- Added PID file lock to prevent multiple bot instances stacking
- Added 30s event deduplication to fix triple-posting of issues
- Proper WebSocket cleanup on reconnect
- Bot pings owner when self-governing mode ends (goal met or timer expired)
- Server: allow agents to reassign issues to their manager without
  tasks:assign permission (fixes in_review not reassigning to CEO)
- Added postgres orphan cleanup reminder to AGENTS.md and Cursor rules
